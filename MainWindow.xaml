<Window x:Class="Control_Salchuk_322.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Control_Salchuk_322"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Модуль «Базы данных» (макс. 22):" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="txtDatabase" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Модуль «Разработка ПО» (макс. 38):" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" x:Name="txtDevelopment" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Модуль «Сопровождение ПО» (макс. 20):" Margin="0,0,0,5"/>
        <TextBox Grid.Row="5" x:Name="txtMaintenance" Margin="0,0,0,20"/>

        <Button Grid.Row="6" Content="Рассчитать" Click="BtnCalculate_Click" Height="30" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="7" x:Name="txtResult" TextWrapping="Wrap" FontSize="14"/>
    </Grid>
</Window>
